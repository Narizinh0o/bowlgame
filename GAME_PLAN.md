# КЛБ Боулинг-Батл — спецификация и план

Фановая браузерная игра-автобатлер на реальных игроках КЛБ. **Отдельный проект**, никак не
связанный с сайтом bowlrus (прод статистики не трогаем). Единственная связь — скрипт
`scripts/build_roster.py`, который копирует снапшот игроков из данных сайта.

**Как продолжить работу в новой сессии Claude Code:** открыть эту папку, сказать
«продолжай по GAME_PLAN.md». Смотреть чекбоксы этапов внизу + `git log`.

---

## 1. Концепция

- Авто-батлер: игрок не бросает шары, а **драфтит команду** из реальных игроков КЛБ
  и смотрит «бой» — симуляцию одной игры в боулинг по системе Бейкера, 5 на 5.
- Режимы: **против компьютера** и **hot-seat** (двое по очереди за одним устройством).
- Бейкер: слот 1 кидает фреймы 1 и 6, слот 2 — 2 и 7, слот 3 — 3 и 8, слот 4 — 4 и 9,
  слот 5 — 5 и 10. Правила боулинга стандартные, 10-й фрейм — до 3 бросков.
- Побеждает команда с большим тоталом. Ничья → дополнительный sudden-death фрейм
  (кидает слот 1, потом слот 2 и т.д., пока не разрешится).

## 2. Данные

- `public/data/roster.json` — снапшот игроков КЛБ: `{id, name, gender, hand, club, avg, games}`.
  - `avg` = Σss/Σg по факт-витрине сайта (с гандикапом — та же формула, что на сайте).
  - `club` — последний клуб игрока (факт с максимальным `seq` турнира).
  - Генерируется: `npm run roster` (нужен соседний репо `../bowlrus` с готовыми JSON).
    Файл коммитится — деплой статический.
- Берём **всех** игроков (даже с 1-2 играми) — недостоверность среднего компенсируется
  сглаживанием и волатильностью (см. ниже).

## 3. Рейтинг и коэффициент

Весь геймбаланс живёт в коде (`src/engine/constants.ts`) — единственный источник правды.
Числа ниже — зафиксированный дизайн; если в коде после калибровки они чуть другие,
правда — в коде.

- **Сглаживание новичков (байес):** `avg_adj = L + (avg − L) · rel`, где
  `rel = games / (games + 12)`, `L` — средний лиги (Σss/Σg по всем).
  Игрок с 2 играми и средним 230 не станет богом — его рейтинг прижмёт к среднему лиги.
- **Базовый рейтинг 30..88:** линейный маппинг `avg_adj` от 2-го перцентиля (→30)
  до максимума (→88), с clamp. Лучший игрок лиги = 88.
- **Редкости** (в драфт-пуле из 30, полный рандом, не зависят от силы):
  4 редких (+3), 2 эпических (+8), 1 легендарный (+15).
- **Клубная синергия** (после расстановки, на весь матч): 3 одноклубника в команде →
  каждому из них +5 рейтинга; 4 → +10; 5 → +20. Остальным — без бонуса.
- `effRating = base + rarity + clubBonus` (теоретический максимум 88+15+20 = 123).
- **Коэффициент:** `skill = effRating − r80`, где `r80` — 80-й перцентиль базовых
  рейтингов ростера («20-е место из 100»). Отображаемый коэф = `1 + skill/40`,
  внутри все формулы — от `skill`.

**ИНВАРИАНТ МОНОТОННОСТИ:** effRating — единственный вход вероятностей. Рост рейтинга
строго ↑ страйк, ↑ все добои, ↓ сплиты/вошауты/желоб, ↑ ожидаемый счёт. Любой бонус
(редкость, клуб) только улучшает игрока. Проверяется автоматически в `npm run sim`.

## 4. Вероятности (база при skill = 0, т.е. у игрока уровня r80)

Первый бросок — категории:

| Категория | База | Модификатор от skill |
|---|---|---|
| Страйк | 50% | `P = 0.5 + 0.4·tanh(skill/37)` — асимптота 90%, не достигается |
| Одна кегля | 25.5% | остаток, вес не модифицируется |
| Мультипин (2+ соседних, реальные комбо) | 15% | остаток, вес не модифицируется |
| Сплит | 5.5% | вес × `1 − 0.35·tanh(skill/45)` (слабее, чем страйк) |
| Вошаут | 2.5% | вес × то же |
| Желоб (стоят все 10) | 0.5% | вес × то же |
| «Дикие» ливы (только физически возможные; ~1 игра из 50–100) | 0.08% | вес × то же |

Не-страйк категории делят `1 − P(страйк)` пропорционально модифицированным весам.

Добой (второй бросок), база × `1 + 0.35·tanh(skill/40)`, кэп 97%:

| Лив | База добоя |
|---|---|
| Одна кегля | 75% |
| Мультипин | 70% |
| Сплит | 20% × индивидуальный множитель сложности (7-10 ≈ ×0.05, бэйби-сплиты ≈ ×1.8) |
| Вошаут | 50% |
| Желоб (все 10) | spare с шансом P(страйк)×0.85, иначе сбито 5–9 |

- Комбинации ливов — только реально возможные, таблицы с весами в `src/engine/leaves.ts`.
  Таблицы заданы для правши, **для левши зеркалятся** (7↔10, 4↔6, 2↔3, 8↔9).
- Если лив не добит: упало `n−1` / `n−2` / 0 кеглей с весами 50/30/20 (перенормировка).
- **Волатильность новичков:** `vol = 1 − rel`; веса плохих категорий × `(1 + 0.6·vol)`,
  добои × `(1 − 0.12·vol)`. Мало игр = дикий, нестабильный игрок.

## 5. Драфт

1. Пул: 30 случайных игроков из всего ростера, 7 слотам пула рандомно раздаются редкости.
2. Кто первый пикает — монетка. Пики строго по очереди: A, B, A, B… по 1 игроку, до 5+5.
3. В карточке пула: имя, клуб, пол, рука, рейтинг (с учётом редкости), редкость.
   Коэффициент и ID не показываем. Клуб виден — можно собирать синергию и красть чужую.
4. После пиков обе стороны расставляют игроков по слотам 1–5, потом старт боя.
5. ИИ: пик ≈ лучший доступный по рейтингу с небольшим рандомом + лёгкий приоритет
   достроить своё клубное комбо / заблокировать чужое. Расстановка: сильнейший — слот 5
   (он кидает 5-й и 10-й фреймы), второй по силе — слот 1.

## 6. Бой (UI)

- Табло Бейкера на две команды: 10 фреймов, классический боулинг-скоринг с бонусами.
- Покадровый авто-плей: бросок → анимация → результат; скорость ×1/×2/пропустить.
- Анимация (этап 5): canvas, дорожка сверху-сзади, шар идёт дугой с «хуком»
  (у левши зеркально), мультяшный боулер (пол → фигурка, рука → сторона замаха),
  кегли падают согласно результату движка. Без детализации лиц и т.п.
- Раунд-итог: победитель, тоталы, MVP (лучший вклад по очкам фреймов).

## 7. Технологии

- Vite + React 19 + TypeScript + Tailwind v3 (тот же стек, что на сайте; БЕЗ роутера —
  фазы игры через стейт-машину: меню → драфт → расстановка → бой → итог).
- Тёмная тема slate-950, акцент amber — стиль семейства bowlrus. Шрифт Inter.
- Движок (`src/engine/`) — чистый TS без DOM: типы, RNG с сидом, рейтинги, генератор
  бросков, бейкер-скоринг. UI только потребляет события движка.
- Деплой: отдельный проект Vercel (подключим, когда будет UI). GitHub-репо создадим
  тогда же.
- Мобильная вёрстка обязательна (как у сайта): viewport, без min-width, брейкпоинт `md:`.

## 8. Команды

```powershell
npm install          # один раз
npm run roster       # обновить снапшот игроков из ../bowlrus (потом закоммитить)
npm run sim          # балансовый прогон движка + проверка монотонности
npm run dev          # дев-сервер
npm run build        # typecheck + прод-сборка
```

## 9. Этапы (чекбоксы — отмечать по мере готовности)

- [x] **Этап 1. Каркас + спека.** Vite/React/Tailwind, GAME_PLAN.md, build_roster.py,
      roster.json сгенерирован.
- [x] **Этап 2. Движок.** `src/engine/*`: рейтинги, вероятности, ливы, бейкер-скоринг,
      RNG с сидом. `npm run sim`: тоталы команд правдоподобны (топ ~230 / медиана ~180 /
      низ ~145), топ бьёт низ 99%, монотонность строгая, скоринг сверен (300/190/90/167).
      Мини-демо в App.tsx (текстовый матч) проверено в браузере.
- [x] **Этап 3. Драфт.** Меню (компьютер / hot-seat), пул 30 с редкостями, пики по
      очереди (монетка решает первого), ИИ-оппонент (макс. прироста ценности команды
      с учётом синергии + блокировка чужого комбо + шум), расстановка тапом-свапом,
      клубные бонусы видны на драфте и расстановке. Проверено в превью: полный цикл
      меню → драфт → расстановка → матч, оба режима, консоль чистая.
- [ ] **Этап 4. Бой без канваса.** Табло Бейкера, покадровый авто-плей текстом/иконками,
      итоговый экран. Игра уже полностью играбельна.
- [ ] **Этап 5. Анимация дорожки.** Canvas: дуга с хуком, зеркало для левши, кегли,
      мультяшный боулер (пол/рука).
- [ ] **Этап 6. Полировка + деплой.** Мобильная вёрстка, GitHub-репо, Vercel.
